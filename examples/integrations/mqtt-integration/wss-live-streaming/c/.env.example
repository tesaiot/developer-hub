# TESAIoT WSS Live Streaming - Environment Configuration
# Copy this file to .env and fill in your values
# Then source it: source .env

# =============================================================================
# MQTT API Token (Required)
# =============================================================================
# Generate from TESAIoT Admin UI:
# 1. Login to https://admin.tesaiot.com
# 2. Go to Organization Settings > MQTT API Tokens
# 3. Click "Generate New Token"
# 4. Copy the token (format: tesa_mqtt_<org>_<32chars>)
export MQTT_API_TOKEN=tesa_mqtt_yourorg_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# MQTT Broker Configuration (Optional)
# =============================================================================
# MQTT broker host (default: mqtt.tesaiot.com)
# export MQTT_HOST=mqtt.tesaiot.com

# WSS port (default: 8085)
# export MQTT_PORT=8085

# Topic to subscribe (default: device/+/telemetry/#)
# Use wildcards: + for single level, # for multi-level
# Examples:
#   device/+/telemetry/#          - All devices, all sensors
#   device/<device_id>/telemetry/#  - Specific device, all sensors
#   device/+/telemetry/temperature  - All devices, temperature only
# export MQTT_TOPIC=device/+/telemetry/#
