# TESAIoT Edge AI Device Simulator - Environment Configuration
# Copy this file to .env and customize if needed

# =============================================================================
# MQTT Broker Configuration
# =============================================================================
# Broker hostname (default: localhost for local development)
MQTT_BROKER_HOST=localhost

# Broker port (default: 1883 for non-TLS MQTT)
MQTT_BROKER_PORT=1883

# =============================================================================
# Device Configuration
# =============================================================================
# Unique device identifier
DEVICE_ID=infineon-sensor-01

# MQTT topic for telemetry data
# Format: device/<device_id>/telemetry
MQTT_TOPIC=device/infineon-sensor-01/telemetry

# =============================================================================
# Simulation Configuration
# =============================================================================
# Interval between telemetry publishes (seconds)
PUBLISH_INTERVAL=1.0

# Inject anomaly every N packets (for Edge AI testing)
# Every 10th packet will contain anomalous values
ANOMALY_INTERVAL=10

# =============================================================================
# Client Configuration (Optional)
# =============================================================================
# Custom client ID (default: auto-generated with timestamp)
# MQTT_CLIENT_ID=my-dps368-simulator
