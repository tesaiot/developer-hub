# TESAIoT Environment Configuration
# Copy this file to .env and update paths for your system:
#   cp .env_example .env
#   nano .env
#
# The runner scripts (run_csr.sh, run_pu.sh) source this file automatically.

# ============================================================================
# OpenSSL 3.x Provider Module Path (REQUIRED)
# ============================================================================
# Path to directory containing trustm_provider.so
# This is needed for OPTIGA Trust M hardware key TLS operations.
#
# Option A: Use bundled provider (recommended)
#   The runner scripts auto-detect lib/ directory, so this may not be needed.
#
# Option B: Use system-installed provider
#   export OPENSSL_MODULES=/usr/lib/aarch64-linux-gnu/ossl-modules
#
# Option C: Use reference build
export OPENSSL_MODULES=/path/to/linux-optiga-trust-m/bin

# ============================================================================
# Trust M Tools Path (optional — auto-detected from binary location)
# ============================================================================
# The library auto-detects tools relative to the binary:
#   <binary_dir>/trustm/trustm_data
#   <binary_dir>/trustm/trustm_protected_update
#
# Set these only if tools are in a non-standard location:
# export TRUSTM_LIB_PATH=/path/to/trustm
# export TRUSTM_DATA_PATH=/path/to/trustm/trustm_data

# ============================================================================
# Logging
# ============================================================================
# Log level: ERROR, WARN, INFO, DEBUG
# Default: INFO
# export TESAIOT_LOG_LEVEL=INFO
